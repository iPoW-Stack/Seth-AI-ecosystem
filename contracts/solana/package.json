{
  "name": "seth-solana-bridge",
  "version": "1.0.0",
  "description": "Seth-Solana Cross-Chain Bridge with 15-50-35 Revenue Distribution",
  "scripts": {
    "build": "anchor build",
    "deploy": "anchor deploy",
    "test": "anchor test",
    "deploy:devnet": "node scripts/deploy-devnet.js",
    "deploy:devnet:init": "node scripts/deploy-devnet.js --init",
    "deploy:full": "node scripts/deploy-devnet.js --init",
    "bridge:init": "node scripts/initialize-bridge.js",
    "pool:init": "node scripts/initialize-pool.js",
    "clean": "rm -rf target node_modules"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@project-serum/anchor": "^0.26.0",
    "@solana/spl-token": "^0.3.8",
    "@solana/web3.js": "^1.87.0",
    "bs58": "^5.0.0",
    "https-proxy-agent": "^7.0.6",
    "undici": "^7.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
